#include "specialize_then_rfactor_each_impl.hpp"
// Member 2 (tile): branch intermediate tiled, fallback intermediate split.
// RED scaffold (same §6 visitation-order gap).
int main() { return main_impl(/*tile=*/true); }
