# How to read this file

These are COVERAGE MILESTONES, not a doc outline and not an ordered to-do list.
Each milestone names a cluster of Halide features that `loopdoc.md` should explain
*correctly and holistically* -- i.e. the doc reads as a coherent whole, and a
micro-agent can reconstruct the loop nest for examples exercising those features
without reading the source.

Because every feature interacts with every other, ordering is a rough guide only.
Skip ahead or double back as judgment dictates.

Marking convention:
  [ ]    not started
  [WIP]  in progress (only one at a time, normally)
  [✅]   the doc explains this well enough that micro-tests covering it pass

"Done" is a judgment call, especially for early milestones -- revisit freely.


# Coverage milestones

[✅] Bootstrap: programming-model overview; basic pure functions; `compute_at`,
      `compute_root`; loop generation from only these; the `print_loop_nest()`
      output format; inlined vs. non-inlined functions.
    EXIT CRITERIA (bootstrap is special -- main agent implements micro_halide directly):
      * A minimal first example (e.g. two pure funcs, one `compute_root`) passes.  [done: two_funcs_root.cpp]
      * `micro_halide` actually implements `print_loop_nest` (no hardcoded output). [done: micro_halide.hpp generates the nest]
      * The `CHEATING` ifdef block is removed from every example (see README rule:
        examples must not branch on USE_MICRO_HALIDE at all; the only ifdef is the
        include/using block at the top of the file).                               [done: box_blur.cpp]
    NOTES:
      * box_blur.cpp may be too rich for a first example (ImageParam,
        repeat_edge with auto-named _0/_1/_2 vars, cast, 4 stages).
        For bootstrapping, you may want to start writing simpler examples first.
        The box_blur.cpp should work correctly (no cheating) at the end of the bootstrap milestone.
      * DONE. Passing examples: two_funcs_root, inline_default, compute_at,
        diamond_root, box_blur, loop_elide_test/_x/_y/_both,
        compute_at_elided_level, tiebreak_realization_order,
        producers_diff_levels, producers_root_and_at. micro_halide builds the
        nest from realization order + produce/consume nesting + per-Func loops;
        loopdoc.md §§1-8 and src_doc/loop_nest_construction.md document/justify it.
      * REALIZATION-ORDER TIE-BREAK: sibling producers of one consumer are
        ordered by (name prefix, first-visitation order, full name) -- NOT by
        left-to-right order in the defining expression. micro_halide now sorts
        producers by this key (was RHS order, which only coincidentally matched).
        loopdoc §4 + src_doc §3 document it; tiebreak_realization_order.cpp tests.
      * MULTI-PRODUCER NESTING: produce/consume blocks of multiple producers of
        one consumer NEST (each consume wraps later producers + the rest), and
        the nesting depth follows each producer's compute level, not the
        dependency graph. Same level -> nested chain in tie-break order; different
        levels -> outer-level producer wraps inner-level producer; root+at mixes
        the two mechanisms. loopdoc §2 (clarified) + §7 (new subsection);
        examples producers_diff_levels.cpp, producers_root_and_at.cpp.
      * NEGATIVE EXAMPLES (illegal compute_at; both Halide and micro_halide must
        error -> harness "Negative example PASS"). Halide's rule: the compute_at
        site must enclose EVERY use of the Func (intersection of the enclosing
        loop stacks across all uses, + root; ComputeLegalSchedules). Three
        structural failures, none bounds-dependent:
          - neg_compute_at_bad_var.cpp: loop var not a dim of the host.
          - neg_compute_at_nonconsumer.cpp: host (realized) does not read f.
          - neg_compute_at_two_consumers.cpp: f used at two root sites, computed
            inside one -> only root is legal. (THE producer/consumer break.)
        micro_halide validates this in LoopNestPrinter::validate (host realized,
        loop exists, every reader enclosed_by the site). loopdoc §7 "When a
        compute_at is illegal" + src_doc §6 document/justify it.
      * compute_at loop ELISION resolved via the design decision below: the loop
        *structure* is derived from the schedule (validated), and point-loop
        elision (extent-1 dims simplified away) is *declared* per example with
        the `micro_halide_collapses(f, {vars})` annotation -- a no-op shim under
        real Halide (halide_compat/halide_compat.h), honored by micro_halide.
        loopdoc §7 documents the qualitative rule, the four loop_elide_* cases,
        and that an elided loop is still a valid compute_at injection site
        (compute_at_elided_level.cpp). README documents the structure-vs-declared
        factoring. This is the standing policy for all future bounds-driven
        elision (incl. RDom/update reduction loops).

[✅] `store_at`, `store_root`
    NOTES: These add "store <func>:" to loop nest output, and require reasoning about illegal store_at.
      * DONE (first delegated milestone). Main agent wrote loopdoc §8, src_doc §8,
        and examples; a logged micro-agent implemented the store-node emission +
        legality in micro_halide from the docs ALONE and all examples passed on
        the first run (integrity review clean: no Halide-source access, read
        loopdoc.md, no commits). Encouraging signal that §8 is clear.
      * Loop-nest effect: a `store f:` node prints only when store level !=
        compute level; it sits at the store level and wraps down to the
        compute-level produce/consume. store_root wraps the whole pipeline body.
        Examples: store_at_compute_at, store_root_compute_at, store_root_chain,
        store_eq_compute (no node), neg_store_inside_compute, neg_store_at_inlined.
      * Sliding-window / storage-folding are value/bounds optimizations with NO
        print_loop_nest structure change, so they are out of scope here (only the
        store node and constant bounds, the latter normalized away, are visible).
        If a later milestone needs to show them, it will be via different output.

[✅] `hoist_storage`, `hoist_storage_root`
    NOTES:
      * FINDING: hoist_storage / hoist_storage_root do NOT change print_loop_nest
        output at all (verified: legal cases print identically to the same
        schedule without hoist). They move the physical allocation without
        triggering sliding window. The ONLY observable effect is errors when
        illegal: (a) hoist on an inlined Func; (b) hoist level inside the store
        level (which must enclose compute). So this milestone is purely about
        legality + a no-op regression guard.
      * DONE (delegated). loopdoc §8 hoist subsection + src_doc §9; micro-agent
        implemented the legality from the docs alone (integrity clean). Examples:
        hoist_storage_noop (positive no-op), neg_hoist_at_inlined,
        neg_hoist_inside_compute. Adversarial follow-ups (main agent) confirmed
        the impl generalizes: store_compute_hoist_all3 (store/compute/hoist all
        distinct, legal, store node + invisible hoist) and neg_hoist_inside_store
        (hoist encloses compute but NOT the store_root level -> correctly
        rejected). All 31 examples pass.

[✅] `split`, `fuse`, `tile`, `reorder`
    NOTES:
      * DONE (delegated). Main agent wrote loopdoc §6 (new) + a §5 rewrite that
        introduces the mutable, innermost-first **dimension list**; src_doc §10;
        and examples. A logged micro-agent implemented all four transforms in
        micro_halide from the docs ALONE -- all examples passed on the first run,
        integrity clean (no Halide-source/src_doc reads, read loopdoc.md, no
        commits), and it reported NO doc gaps. Strong signal §5/§6 are clear.
      * MODEL: each transform rewrites the dim list (which drives both loop
        emission and the set of legal compute_at/store_at site names):
          - split(old,outer,inner,f): replace old with [inner,outer], inner
            innermost -> +1 loop; inner gets a constant bound (normalized away).
          - fuse(inner,outer,fused): remove both, put fused at inner's slot -> -1.
          - reorder(v_in..v_out): permute ONLY the named dims among the slots
            they currently occupy; unnamed dims stay put. PARTIAL reorder (fewer
            vars than dims) is a real, separately-tested code path.
          - tile(x,y,xo,yo,xi,yi,xf,yf) = split(x..)+split(y..)+
            reorder(xi,yi,xo,yo) -> +2 loops, list [xi,yi,xo,yo] innermost-first.
      * VISIBILITY (confirmed): a pure-serial reorder, and the inner/outer order
        of a split, are INVISIBLE to print_loop_nest -- tested only via a
        TOPOLOGICAL CONSEQUENCE (reorder relocates a compute_at producer to a
        different host loop: reorder_topological vs reorder_baseline). Will become
        directly visible once loops carry distinct types (vectorize/parallel/
        unroll), which the canonicalizer DOES keep -- a later milestone.
      * Examples: split_basic, fuse_basic, tile_basic, reorder_baseline,
        reorder_topological, split_compute_at; negatives neg_reorder_bad_var and
        neg_compute_at_fused_away (compute_at at a fuse-consumed var -> only the
        fused var is a legal site). Adversarial follow-ups (main agent, all pass,
        confirm no overfit): reorder_partial, fuse_compute_at (positive: fused
        var IS a legal site), split_twice (split a split-produced var). 42 total.
      * Loop elision is handled by the `micro_halide_collapses` annotation (see bootstrap),
        so this milestone does NOT need to make bounds inference predictable.
        Keep declaring elision via `micro_halide_collapses`; do not try to derive it.
      * split/fuse/reorder DO add/rename/merge loops the schedule controls
        directly -- those are structural and must be derived (not declared).
        Watch the interaction: a split introduces new Vars that can themselves
        be compute_at sites and can be elided; `micro_halide_collapses` keys on Var name,
        so annotate the post-split Var names.
      * GOTCHA (from canonicalize.py, important for designing reorder tests):
        the canonicalizer DROPS loop variable names and keeps only loop TYPE
        (for/parallel/...) + nesting. So a pure serial `reorder` of two plain
        `for` loops is INVISIBLE to the comparison (both sides print just
        `for`/`for`) -- a reorder-only example would pass trivially and prove
        nothing. To actually test reorder, give it a *topological consequence*:
        e.g. reorder so a compute_at producer ends up under a different loop, or
        combine with a loop-type change (vectorize/parallel/unroll, a later
        milestone) that the canonicalizer DOES keep. Same caveat for the order
        of split's inner/outer loops when both are plain serial.
      * split's inner loop is named like the split var; bounds become constant
        (split factor) for the inner loop -- but canonicalize normalizes constant
        bounds away, so only the *number* and nesting of loops is the signal.
        micro_halide will need real Var objects for the new split/fuse vars
        (Var x_outer, x_inner; f.split(x, x_outer, x_inner, factor)) so that
        compute_at/store_at can later name them.

[✅] Update definitions (see e.g. `../apps/hist`)
    NOTES:
      * MODEL (explored, verified against Halide):
        - A Func has an ordered list of STAGES: stage 0 = pure/initial def, then
          one stage per update definition (definition order). All stages are
          emitted inside ONE `produce f` block as consecutive SIBLING loop nests
          (NOT separate produce/consume per stage). A consumer's `consume f`
          wraps the whole produce (it reads the final, post-update value).
        - Stage 0's loops = the Func's pure dimension list (as before, §5/§6).
        - An update stage's loops = its OWN dimension list = the free Vars on its
          LHS + the RVars of any RDom it uses. Default order (innermost-first):
          RVars innermost, and WITHIN the RVars the first-declared (r.x) is the
          INNERMOST (Var convention: first dim fastest -- CORRECTED; an earlier
          note said r.x outer, which was wrong and hidden by name-dropping until
          producer_at_2d_rvar pinned it down). Pure free vars sit outside the
          RVars (first LHS arg innermost among the pures, per §5). A pure
          dimension whose LHS slot holds an RVar/expr (e.g. f(x,r)=... or the
          scatter hist(in(r))) does NOT loop in that stage.
        - RVars print as `for r in [min,max]` -> canonicalize drops the bound, so
          just another `for`. A k-dimensional RDom adds k nested reduction loops.
        - Each stage is scheduled INDEPENDENTLY: `f` schedules stage 0; `f.update(i)`
          returns a Stage handle for update i. split/fuse/reorder/tile (and later
          loop types) apply PER STAGE, on that stage's dim list (which can include
          RVars). E.g. f.update(0).split(x,..) leaves the init stage untouched.
        - compute_at/store_at are per-FUNC: the whole `produce f` (all stages)
          moves to the site. An RVar loop of ANY stage is a valid compute_at site
          for a producer; the producer is injected into that stage's loop.
        - LEGALITY: the §8 legal-site rule now intersects uses across ALL stages.
          A producer computed at an RVar loop (which exists only in its own stage)
          is illegal if the producer is also used in another stage; legal sites
          are the loops common to every stage's use (+ root). Verified:
          neg compute_at(f, r) when p is used in both s0 and s1.
      * OUT OF SCOPE (like bounds inference): reordering RVars in a
        non-associative/non-commutative reduction is illegal, but detecting this
        needs SEMANTIC analysis of the update arithmetic, which micro_halide's
        Expr deliberately discards (it tracks only deps). Document the rule
        qualitatively; do NOT make it a micro_halide-checked test.
      * SCAFFOLDING SPLIT: main agent imitates the API (RDom/RVar/Stage,
        operator+=/*=, second operator= => update stage, Func::update(i)) and
        captures each stage's raw data; micro-agent implements the loop-nest
        EMISSION for stages from the docs. apps/hist uses .update().reorder(...)
        and .rfactor(...) in the wild -- good citation.

      * DONE (delegated; integrity-clean micro-agent run, no doc gaps reported).
        loopdoc.md was reorganized "algorithm-first" and terminology-cleaned, and
        a logged micro-agent implemented stage emission + multi-stage compute_at
        + the non-pure inline default in micro_halide from the docs ALONE. All
        59 examples pass. Final loopdoc order: 1 model, 2 reading, 3 single Func's
        loops AND its stages (update defs = ALGORITHM), 4 TERMINOLOGY (pure/
        non-pure, realized/non-realized, inline-as-level), 5 inlining (pure only),
        6 compute_root, 7 compute_at, 8 store/hoist, 9 split/fuse/reorder/tile,
        10 names, 11 INLINED NON-PURE Funcs (deferred default), 12 assembly.
      * TERMINOLOGY (Halide's own wart, documented §4/§11 + src_doc): "inline" is
        a compute LEVEL name (default), not a synonym for "non-realized". Only a
        PURE inline Func is non-realized (substituted); a non-pure inline Func is
        REALIZED at its innermost use (inline_to_provide). "level"/"site" = a
        (Func,Var) pattern that, with stage unspecified, names a FAMILY of per-
        stage loops; legality = the family covers every read of f (one global
        intersection, not per-stage).
      * NON-PURE INLINE DEFAULT (§11, tested incl. adversarial): equals
        compute_at(consumer, innermost-use) only when all uses share a depth;
        in general it is a per-use-site materialization at each use's innermost
        loop, NOT expressible as one compute_at. Verified by
        nonpure_inline_diff_depths (different depths across stages) and
        nonpure_inline_two_consumers (two distinct consumers); both pass.
      * Examples (all pass): hist_1d, sum_reduction, two_updates, update_2d_rdom,
        update_stage_split, update_stage_reorder, producer_at_rvar,
        func_update_compute_at, store_root_update, update_default_inline,
        weird_histogram_sampling; negatives neg_compute_at_update_rvar; plus
        adversarial producer_at_2d_rvar, cross_stage_compute_at_shared,
        nonpure_inline_diff_depths, nonpure_inline_two_consumers,
        update_split_compute_at. micro_halide: uniform StageData per stage,
        per-stage micro_halide_collapses ((Func)->stage0, (f.update(N))->stage
        N+1), RVar within-order (r.x innermost), use-gated multi-stage injection.

[✅] `rfactor` (factor a reduction for parallelism)
    NOTES:
      * Added as its own milestone (was missing from the original list). It
        builds directly on Update definitions + RDom/RVar, so it must come AFTER
        that milestone. rfactor splits an associative reduction into an
        intermediate Func plus a final combine stage -- it CREATES a new Func and
        new stages, so it has a real print_loop_nest effect (unlike pure reorder).
        Do not attempt before update defs are documented and passing.
      * Added by user: as before, Halide errors due to non-commutative or non-associative
        reorderings are out of scope; don't write such negative examples as
        micro_halide is not expected to reason about this.
      * MODEL (explored against Halide; src/Func.cpp ~857 Stage::rfactor):
        f.update(i).rfactor({{RVar,Var}...}) is a per-update-stage directive that
        CREATES a new Func (auto-named "<orig>_intm") and REWRITES the chosen
        update stage. The `preserved` RVars map to fresh pure Vars.
        - INTERMEDIATE pure/init stage dims (innermost->outermost): original
          Func's pure-stage args, then the new pure Vars in preserved order
          (new Vars OUTERMOST).
        - INTERMEDIATE update stage: a COPY of the original update stage's dim
          list (incl. any prior split/reorder), with each preserved RVar
          replaced in place by its new pure Var; non-preserved RVars stay as the
          intermediate's reduction loops. Same loop order. Reads what the
          original update read.
        - ORIGINAL Func's chosen update stage is rewritten into the MERGE: keeps
          free Vars + preserved RVars (still RVars), DROPS non-preserved RVars,
          and now reads the intermediate -> intermediate becomes a producer of
          the original Func (realization-order slot before it).
        - The intermediate is an ordinary Func with its OWN default schedule:
          non-pure inline (s11) unless compute_root/compute_at'd. Its two stages
          schedule independently via intm and intm.update(0).
      * DOC: new loopdoc s12 "rfactor: factoring an associative reduction into a
        new Func" (inserted after s11 inlined-non-pure; assembly renumbered to
        s13). src_doc new s12 cites Stage::rfactor line-by-line. Cross-refs in
        src_doc remapped (loopdoc assembly s12->s13).
      * EXAMPLES (all 4 match Halide canonical output; micro currently throws the
        TODO stub, expected pre-delegation): rfactor_basic (2D sum, rfactor(r.y,u),
        compute_root), rfactor_default_inline (no compute_root -> intermediate
        realized at use in merge; collapses {x,u}), rfactor_compute_at (intm
        compute_at(f,x); collapses {x}), rfactor_multivar (3-D RDom, preserve
        r.y & r.z, reorder intermediate update).
      * SCAFFOLDING: Stage::rfactor(RVar,Var) and (vector<pair<RVar,Var>>) added
        to micro_halide as a throwing stub; the construction is the documented
        behavior the micro-agent implements. RVar already has name().
      * DEFERRED interaction (NOT in scope this milestone): factoring an RVar
        that was first SPLIT (lesson-18 tiled histogram:
        split(r.x,rxo,rxi).rfactor({{rxo,u}})). Needs (a) split/reorder/tile to
        accept RVar (VarOrRVar) and (b) RVar-ness propagation through splits so
        the merge-drop can recognise rxi as an RVar. rfactor_multivar uses a 3-D
        RDom to get multi-var coverage WITHOUT splitting RVars. See DISCOVERED
        DOC GAPS below.
      * DONE (delegated; integrity-clean micro-agent run add700e5976729ba6, no
        forbidden accesses, read loopdoc.md, no commits; reported NO doc gaps).
        The micro-agent implemented Stage::rfactor from loopdoc s12 + s3 + s11
        ALONE. Notable: it INDEPENDENTLY RE-INTRODUCED a per-stage RVar record
        (StageData::rvars, std::set<string>) -- the same field the human had
        just deleted as dead weight -- because the merge-drop must distinguish
        non-preserved RVars (dropped) from free Vars (kept), and a 1-D RDom's
        RVar is a bare name indistinguishable from a Var otherwise. So the
        experiment confirmed s12+s3 convey that need without source access.
        Impl: pure stage = orig pure dims + new vars (outermost); update = copy
        of orig update dims with preserved RVar->Var, non-preserved stay RVars;
        merge = drop non-preserved RVars, rewire producers (intm reads what the
        update read; merge reads only intm; intm added to f's producer set).
      * ADVERSARIAL follow-ups (main agent, all PASS with ZERO further micro
        changes -> impl generalizes, no overfit): rfactor_func_producer (reduces
        over a real compute_root Func, not an ImageParam -> tests producer
        wiring + realization order g->intm->f), rfactor_preserve_inner (preserve
        the FIRST-declared r.x and lift r.y, mirror of rfactor_basic -> guards
        against hardcoded "drop r.x"), rfactor_second_update (rfactor update(1)
        of a 2-update Func -> tests stage_index handling; trivial reorder on
        update(0) suppresses Halide's all-or-nothing-scheduling warning). 81
        examples total, all pass.
      * VALIDATION SIDE-FIX (during the StageData discussion): added
        update_rvar_decl_order.cpp -- the ONLY example where an RVar's textual
        appearance order (r.y before r.x) differs from RDom declaration order.
        Confirmed Halide orders reduction loops by DECLARATION (r.x innermost),
        not appearance; micro_halide matches. Closes a latent unverified clause
        in s3 ("first-declared innermost") that every prior example left
        untested. s3 wording sharpened to call this out.


[✅] `in` (wrapper functions) and `clone_in`
    NOTES:
      * Behavior + model: loopdoc §13. Source internals (identity, the
        wrappers()-on-wrapped-Func recording, wrap_func_calls, transitive-caller
        normalization, the deep_copy/callee-sharing verdict): src_doc §13.
      * Functionality-neutral STAGE-1 refactor landed: producer reads route
        through LoopNestPrinter::func_producers / stage_producers -- the single
        seam where in()/clone_in() consumer-redirection is to be resolved.
        Rationale + plan: micro_halide/wrappers_refactor_proposal.md + the seam
        comment.
      * Func::in/clone_in are throwing stubs (compile-only) pending implementation.
      * For the micro-agent spawn (settled): (1) implement transitive-caller
        normalization -- f.in(h) reaching f only via g redirects the direct caller
        g (loopdoc §13); (2) resolve the consumer->wrapper swap once up front, not
        per accessor call (seam comment). The realization-order tie-break secondary
        key is now covered by tiebreak_visitation_order and already passes.
      * EXAMPLES WRITTEN + verified vs Halide (in/clone_in positives fail under
        the stub until implemented, like rfactor pre-delegation; the negative
        passes both-error; tiebreak_visitation_order passes now): in_basic,
        in_unscheduled, in_compute_at, in_multi, in_global, in_two_consumers_fix,
        clone_basic, neg_clone_shared_callee, in_transitive, clone_transitive,
        tiebreak_visitation_order.
      * DELEGATED: logged micro-agent implemented in/clone_in from loopdoc §13
        alone (integrity clean; no doc gaps reported by it). Wrappers recorded on
        the wrapped Func, transitive-caller normalization, one-time resolution at
        the seam. 104 examples pass. Main-agent adversarial follow-ups: clone_nonpure
        (clone of a non-pure Func copies both stages -- PASS) and
        in_custom_and_global (custom+global combo -- FAILED, exposed a loopdoc §13
        gap, see DISCOVERED DOC GAPS; §13 fixed, re-delegating that one fix).

[✅] `compute_with` (this is where things get very confusing)
    NOTES:
      * Behavior + model: loopdoc §14 (the constructive "growth" account) + §15
        step 4 (item-based emission). Source internals: src_doc/compute_with.md.
      * DONE (delegated; integrity-clean micro-agent run a3f95deb6b246a5cc:
        no ../src or src_doc/ access, read loopdoc.md only, no commits; reported
        NO doc gaps). Implemented Func/Stage::compute_with from loopdoc §14/§15
        ALONE -- fuse-edge state, union-find grouping, per-stage growth emission,
        item-based injection, owner-keyed collapse, fusion-aware enclosure.
        All examples pass (99 positive + 24 negative, 0 diffs), verified
        independently by the main agent. No previously passing example regressed.
      * Examples (all pass): compute_with_basic/_split/_innermost/_at/_three/
        _parent_alpha/_update/_producer/_chain/_tile/_two_parents/_two_parents_at;
        negatives neg_compute_with_dependency/_dim_count/_level_mismatch/_mismatch/
        _producer_at_child. The two-parent + chain cases (no single group parent)
        and the shared-compute-level requirement are the load-bearing ones.
      * [loopdoc-trace] debug(1) instrumentation kept in src/ScheduleFunctions.cpp
        (build_pipeline_group) as a permanent research aid on this branch.

[ ] `compute_inline`
    NOTES:

[ ] `specialize`
    NOTES: may run into test harness bugs here with loop nest diffing.
    Micro-agent should stop and report problems back to the main agent
    if this is the case.

[ ] `vectorize`, `unroll`, `parallel`, `gpu`, `gpu_lanes`, `gpu_threads`, `gpu_blocks`, `gpu_tile`
    NOTES: micro_halide will not reason about bounds analysis so do
    not write negative examples that fail because of detected race conditions.
    Also skip the gpu_lanes requirement for 32 or fewer iterations.

    Need to think about interaction with compute_with; what happens if
    the two fused stages don't have the same ForMode?

    Need examples with multiple update stages.

    Need mega-example that combines compute_with, multiple update stages, ForMode, and transitivity (indirect producer/consumer dependencies).
    But save this for the adversarial stage.

    TODO: loopdoc.md seems to have made up "host" terminology that isn't used in the real Halide.
    Check if this accusation is accurate.
    If so, this should be changed, because it'll conflict with host/device terms that will matter a lot for GPU.


# DISCOVERED DOC GAPS

When a micro-agent hits a point where `loopdoc.md` does not answer a
concrete question needed to build the loop nest, record it here:

  - [open|fixed] <milestone> | <the specific question the doc couldn't answer> | <example file, if any>

The micro-agent creates [open] issues.
The main-agent may mark issues as [fixed] if changes to the docs seem to have caused later micro-agents to not run into the same confusion.

  - [fixed] bootstrap | how many `for` loops does a `compute_at` Func emit? Dims whose required extent at the compute site is 1 are simplified away. RESOLUTION: not derived (needs bounds inference, out of scope); declared per example via `micro_halide_collapses(f,{vars})` + no-op Halide shim. loopdoc §7 + README document it; examples loop_elide_x/_y/_both and compute_at_elided_level cover it. | loop_elide_*.cpp, compute_at_elided_level.cpp
  - [fixed] update defs | RVar nesting order: §10 originally said the first-declared RVar (r.x) is the OUTERMOST of the reduction loops; it is actually the INNERMOST (Var convention, first dim fastest). Hidden because canonicalize drops loop names (a bare 2-D RDom is symmetric); only a compute_at at a specific RVar exposes it. FIXED §10 + update_2d_rdom comment; producer_at_2d_rvar.cpp pins it down. | producer_at_2d_rvar.cpp
  - [fixed] update defs | When a producer is computed at a loop that appears in SEVERAL stages of the host (e.g. a pure Var present in both the pure and update stages), is it injected into each such stage? Yes -- one produce/consume per stage that has the loop, serving that stage's reads (legality: the site must be a loop present in EVERY reading stage). §10 originally under-specified this; the first update-defs micro-agent wrongly rejected the schedule. FIXED §10 "Compute / store level" subsection. | cross_stage_compute_at_shared.cpp
  - [fixed] compute_at | TRANSITIVE compute_at: f.compute_at(h, v) where h does NOT directly read f -- f is read only through an intermediate g that is itself computed at h's v loop. Halide DOES realize f at h.v (its use, inside g, is enclosed by h.v); the injection gate is "the loop BODY at h.v uses f", and after g is injected there the body transitively calls f. The doc's "What (g,var) points to" wording said f is injected "in each stage that READS f", which is too narrow (direct reads only) -- the first update-defs micro-agent implemented exactly that and so DROPPED f for f.compute_at(h,y). FOUND by main-agent adversarial testing (transitive_compute_at_outer.cpp fails: micro_halide emits no `produce f`). DOC FIXED (§7 "Computing at an indirect consumer's loop" + generalized the use-gating wording to include transitive uses through producers realized in the body). IMPL still open: micro_halide must inject a producer wherever the (post-inner-injection) loop body uses it, not only where the host stage directly reads it. neg_transitive_compute_at_inner.cpp (illegal inner site) and default_inline_nonpure_chain.cpp already pass. NOTE: micro_halide's LEGALITY is transitively correct (all negatives pass, incl. neg_split_transitive_compute_at where xi is in consume g, and neg_split_stage/neg_stage_var); ONLY the emission's use-gate is too narrow. RESOLVED: a logged micro-agent replaced the direct-read use-gate with a transitive "body uses f" test (body_uses/g_uses_f, recursive over the producer graph, gated on producers at this-or-inner host loop so the consume-g case stays illegal). All four went green; integrity clean. Adversarial confirm (also green): transitive_two_level (f->g->k->h, two levels) and direct_and_transitive (h reads f directly AND via g). 73/73 examples pass. | transitive_compute_at_outer / fuse|split|tile_transitive_compute_at / transitive_two_level / direct_and_transitive
  - [open] update defs | A producer injected into MULTIPLE host stages can need DIFFERENT loop elision per injection (e.g. p.compute_at(f,x): in f's pure stage p(x) is a single point -> p's x collapses; in f's update stage p(r) spans a range -> p's x survives). The collapse API is keyed on the PRODUCER's own stages (micro_halide_collapses(p,...) / p.update(N)), which canNOT express "collapse in host-stage-0's injection but not host-stage-1's". STATUS after the update-defs milestone: still open but NARROW -- all committed examples (incl. adversarial nonpure_inline_diff_depths) have SYMMETRIC per-injection elision, which the per-producer-stage API handles. The asymmetric per-host-injection case would need a collapse keyed by (producer, host-stage); deferred as a declared-elision corner (out of scope like bounds inference) until an example needs it. | (asymmetric variant of cross_stage_compute_at_shared, not committed)
  - [open] rfactor | Factoring a reduction var that was first SPLIT (lesson-18 tiled histogram: f.update().split(r.x,rxo,rxi).rfactor({{rxo,u}})). Two missing pieces: (a) the per-stage transforms (split/fuse/tile/reorder) only accept `Var`, not `RVar`/VarOrRVar, so splitting an RVar does not even compile in micro_halide; (b) even given the API, micro_halide tracks RVar-ness via StageData.rvars (the original RDom's RVar names), which split does NOT update -- so the merge-drop step (drop non-preserved RVars) cannot recognise the split halves rxo/rxi as RVars. DECISION: deferred for the rfactor milestone; rfactor_multivar factors whole RVars of a 3-D RDom to get multi-var + reorder coverage WITHOUT splitting an RVar. Documented qualitatively in loopdoc s12 "Legality and limits" and src_doc s12. Revisit when a milestone needs RVar splitting (likely alongside parallel/vectorize loop-type work, which is also when split's purpose becomes visible). | (lesson-18 split+rfactor, not committed; rfactor_multivar is the non-split substitute)
  - [fixed] compute_at | PER-STAGE grounding of INDIRECT (transitive) compute_at injection. f.compute_at(h,v) reached only through an intermediate g (h's body uses g, g uses f). The stage-wildcard LoopLevel(h,v) names a v loop in EVERY stage of h, but g is injected into a stage's body ONLY if that stage actually uses g -- so g pulls f into a stage only where g itself lands. micro_halide's body_uses counted g as present in a stage merely because the stage had a loop matching g's compute level (stage_dim_index(host,hs,g.at_var)>=0), WITHOUT checking g is itself used there (no recursive body_uses(host,hs,g.at_var,g)). FOUND by main-agent adversarial testing at the rfactor x update(1) x indirect-use intersection: rfactor_indirect_at_intm fails -- micro injects h into f_intm's PURE stage (which reads neither g nor f) because the pure stage has a u loop. DOC FIXED (loopdoc s7 new "decided per stage, and only where the intermediate is present" paragraph; src_doc s11 per-stage self-grounding note + retired the stale "gates on direct reads" parenthetical, which the earlier transitive milestone already resolved). IMPL: body_uses must require the intermediate g to be itself used in this stage's body before counting g's reads of f. rfactor_indirect_nested / _h_root / neg_rfactor_indirect_h_at_intm already pass; only rfactor_indirect_at_intm exposes it. RESOLVED: a logged micro-agent (a2cb41857e6e70696, integrity clean, read loopdoc.md, no commits) added the recursive gate `body_uses(host,hs,g.at_var,g)` so g must itself be present in the stage, plus a cycle guard (body_uses_active_ set; two producers at the same (host,v) can query each other -- monotone least-fixpoint, re-entrant query returns false). Derived from s7's "conditions stack" wording; reported no doc gaps. Main-agent adversarial confirm (PASS, zero further micro changes): transitive_multistage_inject (two update stages both read g -> h injected into BOTH, not the pure stage -> guards against an over-narrow pure-stage-only suppression). 86 examples pass. | rfactor_indirect_at_intm.cpp (+ _nested, _h_root, neg_*, transitive_multistage_inject)
  - [fixed] store_at | PER-STAGE placement of the `store` node when the store/compute HOST has several stages. The level (host, v) names a v loop in EVERY stage of host, but the stored Func is computed only in the host stages whose body uses it (the §7 per-stage injection rule), and the `store` node must follow it into exactly those stages. FOUND by main-agent probing (store_at of an rfactor intermediate, and the minimal store_at_update_stage): micro_halide emitted the store node in the FIRST host stage owning the store-level loop (e.g. f's PURE stage) regardless of where the Func is computed, and so omitted it from the stage (the update/merge) that actually produces the Func. DOC FIXED (loopdoc §8 new "per host stage" paragraph; src_doc §8 "Per host stage" subsection). IMPL: micro_halide's store-node emission must attach to the same per-stage body where the produce/consume is injected, not the first stage with a matching loop. store_at_update_stage (plain producer used only in f's update) and rfactor_intm_store_at (rfactor intermediate stored at merge.x) both failed until fixed. RESOLVED: a logged micro-agent (adec909af3977431e, integrity clean, read loopdoc.md, no commits) replaced the single resolve_stage() store-filing with a per-host-stage loop gated by the SAME body_uses predicate used for produce/consume injection (§7), so the store node files into exactly the host stages that compute f. Derived from §8's per-host-stage paragraph; no doc gaps. Main-agent adversarial confirm (PASS, zero further micro changes): store_at_two_update_stages (p used in BOTH update stages -> store node in both, not the pure stage). 91 examples pass. | store_at_update_stage.cpp, rfactor_intm_store_at.cpp, store_at_two_update_stages.cpp
  - [fixed] in/clone_in | GLOBAL wrapper vs f's OWN wrappers. loopdoc §13 said f.in() is "used by every consumer that has no custom wrapper of its own" but did NOT say the global wrapper must SKIP f's own wrappers (custom and global), which keep reading f. FOUND by main-agent adversarial testing (in_custom_and_global): the micro-agent's redirect() sent a custom wrapper's read of f to the global wrapper, producing a chain f->global->custom->g1 instead of custom & global as siblings both reading f. src_doc §13 already had the correct rule (global skips the wrapped Func's own wrappers; custom precedence); the gap was loopdoc §13 only. DOC FIXED (loopdoc §13: custom precedence + global skips f's own wrappers, cite in_custom_and_global). IMPL: redirect() must not apply the global wrapper to any wrapper of f. RESOLVED: a logged micro-agent (ad20a816768fea7ac, integrity clean) added is_own_wrapper(f,c) (c is f's global wrapper or any value in f->wrappers) and gated the global-redirect branch on !is_own_wrapper, so f's own wrappers keep reading f; also gave the custom wrapper a distinct name f_in_<consumer> (the canonicalizer keys identity on the printed name, so two 'f_in' nodes had collapsed). 106 examples pass. | in_custom_and_global.cpp
  - [open] compute_with | The "matching loop nests down to v" criterion includes the KIND of each paired shared dim (Var vs RVar), not just name + count: Halide's per-pair check compares dim_type/for_type/device_api too (ScheduleFunctions ~2496-2514; src_doc compute_with.md). loopdoc §14 now documents the Var/RVar-kind requirement (and that the fuse level itself may be a Var or RVar -- compute_with_rvar.cpp). micro_halide does NOT yet enforce the dim-KIND match (it checks name + count only). Left unimplemented per human instruction (update the doc, defer the micro change to a future micro-agent). NOTE: hard to trigger with a clean example -- it needs a Var and an RVar sharing the same printed name down to v, which normal schedules don't produce; so there is no failing example forcing it. | compute_with_rvar.cpp (positive RVar-fusion; no negative triggers the kind check)

  # --- compute_with hardening findings (from copying Halide's test/correctness/compute_with.cpp; triaged by sub-agent + main-agent verification) ---
  - [open] compute_with OVERFIT | A producer compute_at the GROUP PARENT but read by a fused CHILD is wrongly REJECTED by micro: micro checks compute_at enclosure against the PRE-fusion nest, but §14 says producer legality is §7's enclose-every-use evaluated on the POST-fusion structure (the child's reads sit under the shared loop). loopdoc §14 is correct; micro impl incomplete. | cwtest_nested_compute_with
  - [open] compute_with OVERFIT | micro does not enforce the acyclic stage-order rule (§14 "non-decreasing parent-stage indices"; check_fused_stages_are_scheduled_in_order). Crossing fuse edges (f.s0->g.s1 with f.s1->g.s0, or f.s0->g.s0 & f.s2->g.s0 with f.s1 unfused) are accepted by micro but Halide errors "impossible to establish correct stage order". loopdoc §14 documents the rule; micro impl missing. NOTE: the two neg_cwtest_crossing_edges*.cpp files as written also have a uint8/int literal type mismatch that aborts real Halide on a TYPE error first -- the crossing-edge rejection was confirmed via corrected standalone repros; fix the example types when hardening. | neg_cwtest_crossing_edges1, neg_cwtest_crossing_edges2
  - [open] compute_with OVERFIT | rfactor x compute_with: an external producer (f1) that feeds a MEMBER of a fused group must be ordered before the whole group (the group is one node in realization order, §15 step 2). micro orders f1 AFTER the group. loopdoc correct; micro impl incomplete. | cwtest_update_stage_rfactor
  - [open] compute_with DOC GAP | MEMBER-ORDERING within a fused group: §14 says ties break by §6 name, but the real within-group realization order is a TOPOLOGICAL order over the fuse edges (children before parents => spine-owner last; a CHAIN orders deepest-child-first), with §6 only breaking ties among edge-unordered members. This governs both produce-nesting (wrong for chains) and the order of FREE/unfused stages. Fix §14's member-ordering before re-delegating. | cwtest_mixed_tile_factor (chain), cwtest_overlapping_updates, cwtest_update_stage_diagonal, cwtest_child_var_dependent_bounds
  - [open] compute_with DOC ERROR | §14 currently says compute_at/store_at/hoist_storage naming a CHILD member is ALWAYS illegal ("the child owns no loop there"). FALSE -- verified: it is the ordinary §7 enclose-every-use rule. Naming a child IS legal when the producer's uses are all within that child (the producer lands in that member's region of the shared loop); it is illegal only when a use lies outside (e.g. used by another member). neg_compute_with_producer_at_child.cpp stays a valid negative, but because its producer is used by BOTH f and g, not because child-naming is categorically illegal. Main agent over-generalized this earlier ("verified for all three"). Rewrite §14's ownership/producer bullet. | cwtest_two_compute_at (+ correction to neg_compute_with_producer_at_child rationale)
