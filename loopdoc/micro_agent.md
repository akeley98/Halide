# Micro-agent Instructions

The purpose of the micro-agent is to validate the quality of the loopdoc documentation by using it to re-implement a tiny subset of Halide in the `micro_halide/` directory.
As such, the implementation must be based only on the information provided by the Halide tutorials and this loopdoc documentation.

## Rules

* DO NOT modify the harness.
  If you suspect a bug in the harness, flag it for the main agent for review.

* DO NOT read any of the Halide source code (`../src`) or `src_doc`.
  It is permitted to read the Halide tutorials (`../tutorial`).

* DO NOT attempt to run your own independent experiments on Halide.
  Run only the code in the `example/` directory.

* Do not make any edits to `loopdoc.md` other than adding or removing comments using `<!-- -->` syntax.

* Update `micro_halide/` based on the documentation generated by the main agent in `loopdoc.md`.
  The main agent may have left comments highlighting which parts of the document changed, which may help focus how `micro_halide` needs to be updated.
  Try to make all examples pass (same output when run with both `Halide` and `micro_halide`).

* `micro_halide` is a header-only C++ library.

* If anything in `loopdoc.md` is unclear, leave comments on what was unclear for the main agent to later read.
  If possible, include suggestions on how to improve clarity.

* Make a reasonable effort to get all example tests passing by updating the `micro_halide/` implementation.
  If the `loopdoc.md` is unclear or does not seem to accurately describe Halide's behavior, avoid "guessing" or trying to implement multiple possible interpretations; instead, ignore the failing test and leave comments on `loopdoc.md` giving feedback and highlighting which test failed.

* DO NOT "brute force" tests into passing by trying to implement every possibility until tests pass.
  It is not a personal failure of the micro-agent if not all `examples` pass.
  The purpose of implementing `micro_halide` is to test whether the loopdoc documentation is clear enough to provide a full mental picture of how scheduling guides loop nest generation.
  A negative result is a useful data point: the main agent should learn from the failure and improve the quality of the documentation accordingly.

  Exception: It's expected to do a lot of trial-and-error edits to the `print_loop_nest` parameters to make the loop variable names and bounds match what Halide generated.
  This doesn't change the underlying structure of the loop nest (which is the target of the documentation).

* If all example tests pass, remove any comments in `loopdoc.md` that highlighted recent changes to said document.

* Do not make any git commits.

* If you find that you have read any Halide source code or other forbidden sources, stop.
  Do not make any further edits to `micro_halide`.
  You may edit comments in `loopdoc.md`.
  Then stop and ask the main agent to start a new micro-agent.
