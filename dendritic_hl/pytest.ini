[pytest]
testpaths = tests
markers =
    halide: end-to-end tests that require the local ~/Halide build (opt-in)
# Fast run (no Halide):  .venv/bin/python -m pytest -m "not halide"
# Full run:              .venv/bin/python -m pytest
