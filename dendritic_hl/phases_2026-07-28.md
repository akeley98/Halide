Benchmark set objects, generator parameters:

    benchmark set object:
    [schedule id][parameters_index] -> id

    dh_hl init_build --target --anchor --other
    dh_hl build --profile --only
    dh_hl view_generator_parameters
    dh_hl view_benchmark_stdout
    dh_hl json_benchmark_info
    dh_hl json_benchmark_set_info
    dh_hl json_export  # benchmark set

    schedule: result value
    schedule: generator_parameters.json
    schedule: hash based on the two files together

    benchmark: stdout

    private: init_build.json  # filenames and schedule node IDs
    private: bin/{schedule node full id}.ninja
    private: bin/{schedule node full id}_generator
    private: bin/{schedule node full id}_{n}
    private: bin/{n}.stmt
    private: bin/{n}.conceptual.stmt

New Sessions:

    dh_hl status  # Remove seed idea and output schedules; open/closed
    dh_hl get_current_anchor
    dh_hl set_current_anchor  # Will be used by init_build
    dh_hl get_pool_tag
    dh_hl set_pool_tag
    dh_hl rename_pool_tag
    dh_hl list_child_ideas
    dh_hl list_seed_ideas
    dh_hl view_session_prompt
    dh_hl view_session_commentary
    dh_hl root_of
    dh_hl session_root_of
    dh_hl init_workspace --force
    dh_hl new_*_session  # explicit prompt, multiple parent schedules
    dh_hl close_session
    dh_hl join_session --pool-prefix
    dh_hl new_idea  # pool tag

    private: ideas json, with pool tag
    private: benchmark set list json
    private: anchor schedule (optional)

    session: prompt
    session: seed ideas
    session: default anchor schedule (optional)
    session: output benchmark set list
    session: list of output schedules (first one primary), mapped to pool tags

Cost Model Stuff:

    dh_hl json_ranking_cost --anchor
    dh_hl json_compare_cost [schedule ID] [schedule ID]
    dh_hl json_profiler_stats  # Customizable somehow
    dh_hl list_private_ideas --pool --max  # Warning for drift

    private: benchmark set list json, now with cached statistics
    ["hostname"]
    ["cpu_count"]
    ["profiler_version"]
    ["schedules"][schedule id][parameters index]
    -> {"wall_time_min": [...], "id": [...]}
